{
  "name": "starly-marketing",
  "private": true,
  "description": "Static marketing site for Starly. The single index.html is the page; styles.css is generated from Tailwind by `npm run build`.",
  "scripts": {
    "build": "tailwindcss -i src/tailwind.css -o styles.css --minify",
    "watch": "tailwindcss -i src/tailwind.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
